[providers]
python = "3.12"

[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[variables]
PYTHONPATH = "backend"
